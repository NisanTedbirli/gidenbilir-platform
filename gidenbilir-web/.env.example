# Backend API URL — server-side (Route Handler'lar kullanır)
# Dev: http://localhost:5000
# Prod: https://gidenbilir-api.onrender.com (Render URL)
NEXT_PUBLIC_API_BASE_URL=http://localhost:5000

# Public site URL — sitemap, OG tags, vs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Sentry (production'da, opsiyonel)
# NEXT_PUBLIC_SENTRY_DSN=
# SENTRY_AUTH_TOKEN=
